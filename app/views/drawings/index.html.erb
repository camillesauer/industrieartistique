<div class='connect_btn'>
 <% if user_signed_in? %>
          <%= link_to(image_tag("button.png", alt: "Camille Sauer - all rights reserved."), destroy_user_session_path, method: :delete) %>
        <% else %>
          <%= link_to(image_tag("button.png", alt: "Camille Sauer - all rights reserved."), new_user_session_path) %>
    <% end %>
 </div>
 <div class='top-btn'>
  <img class='gif-t' alt="Camille Sauer - all rights reserved." src="https://res.cloudinary.com/dft5iblyz/image/upload/v1619735463/ezgif.com-gif-maker_l1u729.gif">

 </div>
  <% if user_signed_in? %>
    <div class="btn"><%= link_to "Ajouter", new_drawing_path %></div>
  <% end %>

  <div class="display-grid-card">

    <% @drawings.each do |drawing| %>
      <div class="drawing">
        <% if user_signed_in? %>
          <div class ="up-btn">
            <div class="btn"><%= link_to 'Supprimer', drawing_path(drawing),
              method: :delete,
              data: { confirm: 'Êtes-vous certain de vouloir supprimer?' } %></div>
            <div class="btn"><%= link_to "Editer", edit_drawing_path(drawing.id)%></div>
          </div>
        <% end %>
        <% if drawing.title_2 %>
          <h1 class="title-e"><%= drawing.title_2 %></h1>
        <% end %>
        <% if drawing.title %>
          <h1 class='title-d'><%= link_to drawing.title, drawing_path(drawing)%></h1>
        <% end %>
      </div>
    <% end %>

  </div>


